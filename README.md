# Windows Fix Tool (PyQt5)

Simple Windows GUI to run pre-solve PowerShell scripts with live logs.

## Setup
```bash
python -m venv .venv
. .venv/Scripts/activate
pip install -r requirements.txt
```

## Run
```bash
python src/main.py
```

## Add scripts

Put your .ps1 files in ./scripts.
